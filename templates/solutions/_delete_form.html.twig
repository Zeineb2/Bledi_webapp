<form method="post" action="{{ path('app_solutions_delete', {'idSol': solution.idSol}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solution.idSol) }}">
    <button class="btn">Delete</button>
</form>