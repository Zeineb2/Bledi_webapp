{% extends 'base.html.twig' %}

{% block title %}Edit Solutions{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Solutions</h1>
        <div class="row">
            <div class="col-md-6">
                {{ include('solutions/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
            <div class="col-md-6">
                {{ include('solutions/_delete_form.html.twig') }}
                <a href="{{ path('app_solutions_index') }}" class="btn btn-secondary mt-3">Back to List</a>
            </div>
        </div>
    </div>
{% endblock %}
