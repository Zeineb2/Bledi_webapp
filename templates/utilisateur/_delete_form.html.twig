<form method="post" action="{{ path('app_utilisateur_delete', {'cin': utilisateur.cin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.cin) }}">
    <button class="btn">Delete</button>
</form>
