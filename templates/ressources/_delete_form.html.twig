<form method="post" action="{{ path('app_ressources_delete', {'idRessource': ressource.idRessource}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.idRessource) }}">
    <button class="btn">Delete</button>
</form>
