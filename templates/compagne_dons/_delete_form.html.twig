<form method="post" action="{{ path('app_compagne_dons_delete', {'id': compagne_don.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compagne_don.id) }}">
    <button class="btn">Delete</button>
</form>
